/*Node js: Se creo cuando surgió la necesidad de ejecutar javascript fuera
del navegador */

let dias = ['lunes', 'martes', 'miércoles', 'jueves', 'viernes', 'sábado', 'domingo'];

//devolver array pasado a mayus

const mapArray = dias.map((elem) => elem.toUpperCase());

console.log(mapArray);
